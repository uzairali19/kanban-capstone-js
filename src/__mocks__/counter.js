function numberOfReservations(content) {
  let myCount = content.length;
  // const resNo = document.getElementById('resNo');
  if (myCount === undefined) {
    myCount = 0;
  }
  return myCount;
}

export default numberOfReservations;
